{
  "name": "data-client",
  "version": "0.0.1",
  "description": "Frontend for a data client",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jeremy Herbst",
  "license": "MIT",
  "devDependencies": {
    "vite": "^3.2.1"
  }
}