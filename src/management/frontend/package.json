{
  "name": "user-management",
  "version": "0.0.1",
  "description": "Frontend for user-management application",
  "scripts": {
    "dev": "vite --host --port 5174",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jeremy Herbst",
  "license": "MIT",
  "devDependencies": {
    "vite": "^3.2.1"
  }
}